import { computeSelections, getSelectionsString } from "./helpers";

describe("Helpers", () => {
  it("computes selections", () => {});

  it("generates selections string", () => {
    expect(getSelectionsString()).toEqual("Nothing selected");
  });
});
